# Production Environment Configuration

# Database Configuration
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# OpenAI Configuration
OPENAI_API_KEY=sk-proj-your-production-key-here
OPENAI_MODEL=gpt-4

# Authentication Configuration
JWT_SECRET=your-production-jwt-secret-min-32-chars
BETTER_AUTH_SECRET=your-production-better-auth-secret-min-32-chars

# Application Configuration
ENVIRONMENT=production
LOG_LEVEL=WARNING
CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Database Connection Pool (Production Settings)
DB_POOL_SIZE=20
DB_MAX_OVERFLOW=10
DB_POOL_TIMEOUT=30
DB_POOL_RECYCLE=3600

# Performance Configuration
AGENT_TIMEOUT=30
MAX_CONVERSATION_HISTORY=20
RATE_LIMIT=60

# Monitoring Configuration
SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id
ENABLE_PERFORMANCE_MONITORING=true
ENABLE_QUERY_LOGGING=false

# Security Configuration
ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com
SECURE_COOKIES=true
HTTPS_ONLY=true

# Worker Configuration (Gunicorn)
WORKERS=4
WORKER_CLASS=uvicorn.workers.UvicornWorker
WORKER_TIMEOUT=120
MAX_REQUESTS=1000
MAX_REQUESTS_JITTER=50

# Notes:
# - Replace all placeholder values with actual production credentials
# - Never commit this file to version control
# - Store secrets in secure secret management service
# - Rotate secrets regularly (every 90 days)
# - Use strong random strings for JWT_SECRET and BETTER_AUTH_SECRET
# - Generate secrets: openssl rand -hex 32
